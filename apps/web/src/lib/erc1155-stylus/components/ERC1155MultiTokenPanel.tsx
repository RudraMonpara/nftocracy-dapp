'use client';

/**
 * Legacy scaffold component (not wired). Prefer the voting UI under / and /proposals.
 */
export function ERC1155MultiTokenPanel() {
  return (
    <div className="rounded-lg border border-zinc-800 bg-zinc-900/50 p-4 text-sm text-zinc-500">
      Multi-token panel is not used in this app build. Use the main voting flows instead.
    </div>
  );
}
